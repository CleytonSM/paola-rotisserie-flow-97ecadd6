export type Client = {
  id: string;
  name: string;
  cpf_cnpj?: string;
  email?: string;
  phone?: string;
};

export type FormData = {
  name: string;
  cpf_cnpj: string;
  email: string;
  phone: string;
};

