export type Supplier = {
  id: string;
  name: string;
  cnpj?: string;
  email?: string;
  phone?: string;
};

export type FormData = {
  name: string;
  cnpj: string;
  email: string;
  phone: string;
};

