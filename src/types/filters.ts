/**
 * Common filter types used across table components
 */

export type StatusFilter = "all" | "pending" | "paid" | "received" | "overdue";
export type AccountStatus = "pending" | "paid" | "received" | "overdue";
